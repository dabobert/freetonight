<div id="articles">
	<h2 id="month"><%= @date.strftime("%B %Y") %></h2>

	<%= hours(@availabilities, true) %>
	
	<%= weekly_calendar @date do |date| %>
    <%= hours @availabilities %>
	<% end %>
</div>