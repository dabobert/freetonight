<div>
  <%= debug @plans %>
</div>
<div id="articles">
	<h2 id="month"><%= @date.strftime("%B %Y") %></h2>

	<%= hours(@availabilities, :label=>true) %>
	
	<%= weekly_calendar @date do |date| %>
    <%= hours @availabilities, :events => @plans[date] %>
	<% end %>
</div>